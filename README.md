# Primitive DB - Консольная база данных на основе JSON

## Описание
Primitive DB - это консольное приложение для работы с табличной базой данных,
основанной на файлах JSON. Приложение поддерживает создание, удаление таблиц
и основные операции с данными через интуитивно понятный CLI интерфейс.

## Структура проекта

project#2_<фамилия>_<группа>/
├── src/primitive_db/
│   ├── __init__.py      # Экспорт модуля
│   ├── constants.py     # Константы и сообщения
│   ├── core.py          # Ядро БД: Database, Table, Column, Row
│   ├── decorators.py    # Декораторы и кэширование
│   ├── engine.py        # CLI интерфейс
│   ├── main.py          # Точка входа
│   ├── parser.py        # Парсер команд
│   └── utils.py         # Вспомогательные функции
├── data/                # Директория данных
│   ├── tables/          # Файлы таблиц (.json)
│   └── db_meta.json     # Метаданные БД
├── Makefile             # Автоматизация
├── pyproject.toml       # Конфигурация Poetry
└── README.md            # Документация


### Требования
- Python 3.11 или выше
- Poetry 1.2.0 или выше (пакетный менеджер)
- Windows 11 / Linux / macOS

## Используемые технологии
Python 3.10+ - основной язык

Poetry - управление зависимостями

Ruff - линтинг и форматирование

PrettyTable - красивый вывод таблиц

JSON - хранение данных

Декораторы - расширение функциональности

Замыкания - реализация кэширования

## Особенности
✅ Полная поддержка CRUD-операций
✅ Кэширование запросов (TTL: 5 минут)
✅ Атомарные операции записи
✅ Красивый табличный вывод (PrettyTable)
✅ Подтверждение опасных операций
✅ Логирование времени выполнения
✅ Обработка ошибок с детальными сообщениями
✅ Автоинкремент ID
✅ Поддержка типов данных: int, str, bool


### Установка из исходного кода
Быстрый старт
Установка из PyPI (если доступно)      bash

pip install project-2_avo_m25_555

# Клонирование репозитория
git clone <https://github.com/avolkon/project-2_avo_m25_555>
cd project-2_avo_m25_555

# Установка зависимостей
make install

# Сборка пакета
make build

# Установка пакета в систему
make package-install

make lint  # должен пройти без ошибок
make run    # запуск базы данных

Запуск

# После установки напрямую через Poetry
poetry run project

# Или через Makefile
make run


## Доступные команды
### Makefile команды

make install         # Установить зависимости
make build           # Собрать пакет
make package-install # Установить пакет в систему

make run             # Запуск приложения

make lint            # Проверка кода линтером
make fix             # Исправление ошибок кода линтером
make format          # Форматирование кода линтером
make publish         # Сухая публикация
make test            # Запуск тестов (если есть)


### Управление таблицами

create_table <таблица> <столбец:тип> ...    -- Создать таблицу
drop_table <таблица>                        -- Удалить таблицу
list_tables                                 -- Список всех таблиц
info <таблица>                              -- Информация о таблице

### Операции с данными

insert into <таблица> values (<значение1>, ...)          -- Добавить запись
select from <таблица> [where <условие>]                  -- Выбрать записи
update <таблица> set <столбец>=<значение> where <условие> -- Обновить записи
delete from <таблица> where <условие>                    -- Удалить записи

### Общие команды

help    -- Показать справку
exit    -- Выйти из программы

## Примеры использования

### Создание таблицы

>>> create_table users name:str age:int is_active:bool
✅ Успешно: Таблица "users" успешно создана со столбцами: ID:int, name:str, age:int, is_active:bool

### Добавление данных

>>> insert into users values ("Иван", 25, true)
✅ Успешно: Запись с ID=1 успешно добавлена в таблицу "users". ⏱️ Время выполнения: 0.001 сек

### Выборка данных

>>> select from users where age > 20
✅ Успешно: Найдено 2 записей в таблице 'users'

+----+-------+-----+-----------+
| ID | name  | age | is_active |
+----+-------+-----+-----------+
| 1  | Иван  | 25  |   True    |
| 2  | Мария | 28  |   True    |
+----+-------+-----+-----------+
📊 Кэш-попадание для ключа: table_users

### Обновление данных

>>> update users set age = 26 where name = "Иван"
✅ Успешно: Обновлено 1 записей в таблице "users".
⚠️  Вы уверены, что хотите выполнить "обновление данных"? [y/N]: y

### Удаление данных

>>> delete from users where ID = 1
✅ Успешно: Удалено 1 записей из таблицы "users".
⚠️  Вы уверены, что хотите выполнить "удаление данных"? [y/N]: y

## Настройка и конфигурация
### Конфигурационные константы (constants.py)

## Особенности реализации

### Архитектура
Паттерн "Команда": Каждая операция представлена классом-командой

Атомарные операции: Запись через временные файлы

Кэширование: TTL-кэш с замыканиями

Валидация: Проверка типов данных при вставке и обновлении

### Основные классы
Database: Основной класс базы данных

Table: Представление таблицы

Column: Определение столбца с типом

Row: Строка данных

Command: Абстрактный класс команды

### Декораторы
@handle_db_errors: Обработка ошибок базы данных

@confirm_action: Подтверждение опасных операций

@log_time: Логирование времени выполнения

create_cacher(): Фабрика для создания кэшера

## Хранение данных
Форматы файлов
Метаданные: data/db_meta.json (структура таблиц)

Данные таблиц: data/tables/<table_name>.json

Автоматическое сохранение: После каждой операции

## Типы данных
int: Целочисленные значения

str: Строковые значения (в кавычках)

bool: Логические значения (true/false)

## Обработка ошибок
Система предоставляет понятные сообщения об ошибках:

Отсутствие таблицы или столбца

Несоответствие типов данных

Ошибки синтаксиса команд

Проблемы с доступом к файлам

## Производительность
Кэширование запросов: 5 минут TTL

Индексация столбцов: Быстрый доступ по именам

Оптимизация памяти: Загрузка данных по требованию

Пакетные операции: Эффективная обработка множества записей

## Тестирование и качество кода
Линтинг

bash
make lint  # Проверка стиля кода с Ruff

## Требования к коду
Соответствие PEP 8

Документация функций

Осмысленные имена переменных

Отсутствие "магических чисел"

Лицензия
MEPHI License

Авторы
Анастасия Волконская

Поддержка (психологическая:)
http://tg.me/avolkon 
